{
  "description": "Choose a model and reasoning effort for unpinned Codex subagents.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^(Agent|spawn_agent)$",
        "hooks": [
          {
            "type": "command",
            "command": "python -I -m codex_model_router --hook",
            "commandWindows": "py -3 -I -m codex_model_router --hook",
            "timeout": 55,
            "statusMessage": "Choosing the subagent model"
          }
        ]
      }
    ]
  }
}
